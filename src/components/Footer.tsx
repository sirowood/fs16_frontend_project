import { Box } from '@mui/material';

// TODO
const Footer = () => {
  return (
    <Box
      component="footer"
      sx={{
        padding: '12px 24px',
        bgcolor: 'background.default',
        color: 'text.primary',
      }}
    >
      Footer
    </Box>
  );
};

export default Footer;
